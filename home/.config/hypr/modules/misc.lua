hl.config({
    misc = {
        focus_on_activate = true,

        -- Wake monitors from DPMS on mouse/key activity.
        mouse_move_enables_dpms = true,
        key_press_enables_dpms = true,

        -- Disable primary-selection paste; regular Ctrl+C/Ctrl+V remains unaffected.
        middle_click_paste = false,
    },
})
